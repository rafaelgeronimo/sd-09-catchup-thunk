import * as fruitAction from './fruitAction';
import * as productAction from './productAction';
import * as weatherAction from './weatherAction';

export { fruitAction, productAction, weatherAction };
